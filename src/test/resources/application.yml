spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/emsp_db  # 指向 Docker 容器
    username: emsp
    password: emsp123
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: false
